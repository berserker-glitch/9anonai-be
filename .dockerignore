# Dependencies — rebuilt inside the container via npm ci
node_modules

# Build output — rebuilt inside the container via npm run build
dist

# Environment files — injected by Coolify at runtime
.env
.env.*

# Git and editor artifacts
.git
.gitignore
*.md

# OS junk
Thumbs.db
.DS_Store

# Logs
logs
*.log
npm-debug.log*
